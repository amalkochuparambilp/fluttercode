import 'package:flutter/material.dart';

class SemOne extends StatelessWidget {
  const SemOne({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Edoo | BCA'),
      ),
      body: const Center(
        child: Text('Coming Soon...'),
      ),
    );
  }
}

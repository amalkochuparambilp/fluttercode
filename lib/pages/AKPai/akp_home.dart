import 'package:flutter/material.dart';

class AkpHome extends StatelessWidget {
  const AkpHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Edoo | AKP.ai'),
      ),
      body: const Center(
        child: Text('Coming Soon...'),
      ),
    );
  }
}
